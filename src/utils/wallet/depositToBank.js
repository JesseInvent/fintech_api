

/**
 * Main feature:
 * Request an external payment gateway to transfer to a bank account
 */

export default async ({accountNumber, bankName, sortCode}) => {

    // request external payment API

    return true
}